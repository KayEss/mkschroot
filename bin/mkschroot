#!/usr/bin/env python
import os
from simplejson import load
import sys
import tempfile

from mkschroot import build_from_config
    

def main(json_file):
    config = load(file(json_file, 'r'))
    build_from_config(config)


if __name__ == '__main__':
    main(sys.argv[1])

